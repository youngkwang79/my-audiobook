import os
import time
import re
import random
import sys
from google import genai

sys.stdout.reconfigure(encoding='utf-8')
client = genai.Client(api_key='[GOOGLE_API_KEY]')
MODEL_NAME = 'gemini-2.5-flash'

def generate_academy(prompt, description="요청"):
    for attempt in range(1, 4):
        try:
            response = client.models.generate_content(model=MODEL_NAME, contents=prompt)
            if response and response.text and response.text.strip(): return response.text
            time.sleep(2)
        except Exception: time.sleep(3)
    return "아카데미 연무장의 침묵이 깨졌다. 무명 낙제생의 일격에 천재 제자가 바닥에 쓰러졌다."

print("==================================================")
print("🏫 [무림북 아카데미 사이다 리액션 자동화 가동] 🏫")
print("==================================================")

ACADEMY_POOL = [
    "엘리트 마법 아카데미의 만년 낙제생이자 평민인 주인공이 고대 주술의 해독법을 각성하여 실기 평가 날 오만한 공작가 천재 제자를 한 주먹에 기절시키는 스토리",
    "소설 속 주인공들을 빛내주는 이름 없는 '아카데미 경비원의 아들' 엑스트라로 빙의했으나, 실전 압축 생존 스킬로 아카데미를 기습한 악마 숭배자들을 뒤에서 혼자 도륙하는 스토리",
    "아카데미의 검술 둔재로 놀림당하던 소년이 칼을 휘두를 때마다 숙련도가 100배씩 쌓이는 사기 패시브를 얻어 중간고사 수석을 차지하고 전 학교를 뒤집어놓는 스토리",
    "아카데미 수석 입학자인 귀공녀 여주인공의 집사이자 노예였던 소년이, 숨겨진 흑마법 면역 스킬을 터뜨려 폭주하는 마법 던전에서 여주인공을 구하고 전교생의 우상이 되는 스토리",
    "자신이 아카데미에서 펼치는 모든 대결과 무공이 현대 커뮤니티 사이트에 웹툰처럼 실시간 연재되며 게시판 댓글 창 유저들의 팁을 받아 초고속 성장하는 특이한 인방물 스토리"
]

HISTORY_FILE = "academy_history.txt"
# novel_title = "예시제목" # 이어 쓰기 시 '#' 지우고 폴더명 넣기

if 'novel_title' not in locals():
    used_plots = []
    if os.path.exists(HISTORY_FILE):
        with open(HISTORY_FILE, "r", encoding="utf-8") as h_f:
            used_plots = [line.strip() for line in h_f.readlines() if line.strip()]
    available_plots = [p for p in ACADEMY_POOL if p not in used_plots]
    if not available_plots:
        used_plots = []; available_plots = ACADEMY_POOL.copy()
        if os.path.exists(HISTORY_FILE): os.remove(HISTORY_FILE)
    selected_plot = random.choice(available_plots)
    with open(HISTORY_FILE, "a", encoding="utf-8") as h_f: h_f.write(selected_plot + "\n")

    prompt_msg = f"""당신은 아카데미물 장르의 신이라 불리는 웹소설 작가입니다. 다음 플롯을 바탕으로 소설을 기획하십시오. 한자 절대 금지.
    플롯: {selected_plot}
    제목 공식: 아카데미 꼴찌가 신공을 숨김, 나 혼자 엑스트라 가문 서자
    출력 형식:
    제목: [작성]
    소개글: [작성]
    전체 줄거리: [작성]"""
    concept_text = generate_academy(prompt_msg, "기획 생성")
    novel_title, novel_intro, overall_plot = "무명 아카데미", "아카데미 대작", ""
    for line in concept_text.split('\n'):
        if line.startswith("제목:"): novel_title = line.replace("제목:", "").strip()
        if line.startswith("소개글:"): novel_intro = line.replace("소개글:", "").strip()
        if line.startswith("전체 줄거리:"): overall_plot = line.replace("전체 줄거리:", "").strip()
else: novel_intro, overall_plot = "이어서 쓰는 중", "이어서 쓰는 중"

safe_title = re.sub(r'[^\w\s-]', '', novel_title).strip()
output_dir = f"무림북_아카데미_{safe_title}"
if not os.path.exists(output_dir): os.makedirs(output_dir)

prompt_1_25 = f"소설 <{novel_title}> (줄거리: {overall_plot})의 1~25화 세부 시놉시스를 '1화: 내용' 형식으로 적어줘. 순수 한글만 사용."
response_1_25 = generate_academy(prompt_1_25, "시놉1")
prompt_26_50 = f"소설 <{novel_title}> (줄거리: {overall_plot})의 26~50화 세부 시놉시스를 '26화: 내용' 형식으로 적어줘. 순수 한글만 사용."
response_26_50 = generate_academy(prompt_26_50, "시놉2")
full_synopsis = response_1_25 + "\n" + response_26_50

synopses = {}
for i in range(1, 51):
    search_key = f"{i}화:"
    if search_key in full_synopsis:
        part = full_synopsis.split(search_key)[1]
        next_key = f"{i+1}화:"
        synopses[i] = part.split(next_key)[0].strip() if next_key in part else part.split("\n\n")[0].strip()
    else: synopses[i] = f"{novel_title}의 파란만장한 제 {i}화 스토리가 전개됩니다."

RULES = """틀딱 말투 금지. 빠른 템포의 웹소설 문체(~했다) 사용. 한자 표기 절대 금지. 아카데미 교복, 마법석 시험, 주인공의 실력에 기겁하는 동급생들의 리액션과 인터넷 익명 게시판 반응(예: [제목] 오늘 자 연무장 낙제생 대결 본 사람 있냐? ㅋㅋㅋ)을 감칠맛 나게 섞어 서술하십시오."""

previous_summary = "없음"
for chapter in range(1, 51):
    filename = os.path.join(output_dir, f"{chapter:02d}화_{safe_title}.txt")
    if os.path.exists(filename):
        print(f"⏭️ [{chapter}/50화] 이미 집필된 파일 패스 (이어 쓰기 작동)"); continue
    
    print(f"▓ [{chapter}/50화] 아카데미 본문 폭풍 생성 중... 🔥")
    prompt = f"{RULES}\n제목: {novel_title}\n이전 요약: {previous_summary}\n이번화 시놉시스: {synopses[chapter]}\n{chapter}화 본문을 집필해줘."
    chapter_text = generate_academy(prompt, "본문")
    with open(filename, "w", encoding="utf-8") as f: f.write(chapter_text)
    previous_summary = generate_academy(f"다음 본문을 3줄 요약해줘.\n\n[본문]\n{chapter_text}", "요약")
    time.sleep(1)
print("\n==================================================\n🎉 아카데미 완결!\n==================================================")